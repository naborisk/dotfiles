#!/bin/bash

INF="tun0 br0 eth0"

for i in $INF; do
    IP=$((ip show $i || ifconfig $i) 2>/dev/null | grep -m 1 inet | awk '{print $2}' | cut -d / -f 1)
    [ -n "$IP" ] && { echo $i \[$IP\]; exit 0; }
done
